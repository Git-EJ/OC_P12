import { createContext } from "react";

/**
 * 
 * @returns {JSX.Element}
 * @description This component is used to create the ApiContext.
 * 
 */

const ApiContext = createContext();

export default ApiContext;
